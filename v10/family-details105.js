import {esc} from './engine.js';
import {inferTime86} from './time-place86.js?v=86';
import {matrix105,matrixFacts105} from './matrix105.js?v=105';
import {routeQuestions105} from './question-router105.js?v=105';
const parse=v=>Array.isArray(v)?v:String(v||'').split('|||').filter(Boolean),vals=(s,id)=>parse(s.metadata?.familyDetails?.[id]);
function fmt(unit,n=1){n=Math.max(1,Number(n)||1);if(unit==='حصة')return n===1?'حصة واحدة':n===2?'حصتان':`${n} حصص`;if(unit==='يوم')return n===1?'يوم واحد':n===2?'يومان':`${n} أيام`;if(unit==='أسبوع')return n===1?'أسبوع واحد':n===2?'أسبوعان':`${n} أسابيع`;if(unit==='فصل دراسي')return'فصل دراسي';if(unit==='مستمر')return'مستمر';return''}
function parseTime(v=''){const x=String(v);if(x.includes('فصل دراسي'))return{unit:'فصل دراسي',count:1};if(x.includes('مستمر'))return{unit:'مستمر',count:1};if(/حص/.test(x))return{unit:'حصة',count:x.includes('حصتان')?2:Number(x.match(/\d+/)?.[0]||1)};if(/يوم|أيام|يومان/.test(x))return{unit:'يوم',count:x.includes('يومان')?2:Number(x.match(/\d+/)?.[0]||1)};if(/أسبوع|اسبوع|أسابيع|أسبوعان/.test(x))return{unit:'أسبوع',count:x.includes('أسبوعان')?2:Number(x.match(/\d+/)?.[0]||1)};return{unit:'',count:1}}
function inferred(s,m){if(s.metadata?.durationChoice)return{...parseTime(s.metadata.durationChoice),source:'user'};const x=s.metadata?.semantic101?.duration;if(x)return{...parseTime(x),source:'fact'};if(m?.suggestedDuration)return{...parseTime(m.suggestedDuration),source:'inference'};const t=inferTime86(s.raw||'',s.classification?.type)||{};return{...parseTime(t.duration||t.period||t.frequency||''),source:t.duration||t.period||t.frequency?'inference':''}}
function durationBlock(s,m){const got=inferred(s,m),unit=got.unit,count=got.count||1,value=fmt(unit,count);return `<div class="chooser durationContext v103Duration" data-time-unit="${esc(unit)}" data-time-count="${count}"><small>المدة</small><p class="questionHelp">${value?`<b>${got.source==='fact'?'فهمت المدة من وصفك':got.source==='inference'?'مدة مقترحة من السياق':'المدة'}:</b> ${esc(value)}. عدّلها إذا احتجت.`:'اختر المدة فقط إذا كانت مهمة ولم تذكرها.'}</p><input type="hidden" id="durationSuggested" value="${esc(value)}"><input type="hidden" id="customDuration" value=""><div class="chips choiceGrid"><button type="button" class="chip ${unit==='حصة'?'on':''}" data-duration-unit="حصة">حصة</button><button type="button" class="chip ${unit==='يوم'?'on':''}" data-duration-unit="يوم">يوم</button><button type="button" class="chip ${unit==='أسبوع'?'on':''}" data-duration-unit="أسبوع">أسبوع</button><button type="button" class="chip ${unit==='فصل دراسي'?'on':''}" data-duration-unit="فصل دراسي">فصل دراسي</button><button type="button" class="chip ${unit==='مستمر'?'on':''}" data-duration-unit="مستمر">مستمر</button></div><div class="timeCounter" style="${['حصة','يوم','أسبوع'].includes(unit)?'':'display:none'};margin-top:12px;align-items:center;gap:12px"><button type="button" class="btn" data-duration-step="-1">−</button><strong data-time-count-label>${count}</strong><button type="button" class="btn" data-duration-step="1">+</button></div></div>`}
function refreshTime(box){const unit=box.dataset.timeUnit||'',count=Math.max(1,Math.min(99,Number(box.dataset.timeCount)||1)),value=fmt(unit,count);box.dataset.timeCount=count;box.querySelectorAll('[data-duration-unit]').forEach(b=>b.classList.toggle('on',b.dataset.durationUnit===unit));const counter=box.querySelector('.timeCounter');if(counter)counter.style.display=['حصة','يوم','أسبوع'].includes(unit)?'flex':'none';const label=box.querySelector('[data-time-count-label]');if(label)label.textContent=count;const hidden=box.querySelector('#durationSuggested');if(hidden)hidden.value=value;box.querySelector('#customDuration')?.dispatchEvent(new Event('input',{bubbles:true}))}
document.addEventListener('click',e=>{const ub=e.target.closest('[data-duration-unit]');if(ub){const box=ub.closest('.durationContext');if(box){box.dataset.timeUnit=ub.dataset.durationUnit;box.dataset.timeCount='1';refreshTime(box)}return}const st=e.target.closest('[data-duration-step]');if(st){const box=st.closest('.durationContext');if(box){box.dataset.timeCount=String(Math.max(1,(Number(box.dataset.timeCount)||1)+Number(st.dataset.durationStep)));refreshTime(box)}return}const b=e.target.closest('[data-family-pick]');if(!b)return;const box=b.closest('.familyChoice'),hidden=box?.querySelector(`[data-family-field="${CSS.escape(b.dataset.familyPick)}"]`);if(!hidden)return;let a=parse(hidden.value),max=Number(box.dataset.max||3),v=b.dataset.familyValue;a=a.includes(v)?a.filter(x=>x!==v):(max===1?[v]:[...a,v].slice(-max));hidden.value=a.join('|||');box.querySelectorAll('[data-family-pick]').forEach(x=>x.classList.toggle('on',a.includes(x.dataset.familyValue)));hidden.dispatchEvent(new Event('input',{bubbles:true}))},true);
function summary(m){const bits=[m.family,m.subject,m.branch,m.topic,m.skill,m.domain,m.context].filter(Boolean);return `<div class="matrixSummary103"><small>ما فهمه المحرك</small><div>${[...new Set(bits)].map(x=>`<span>${esc(x)}</span>`).join('')}</div><p>الموضوع العام والمهارة الدقيقة منفصلان الآن؛ لذلك لن يعتبر «الكسور» مثلًا جوابًا كافيًا عن نوع الصعوبة.</p></div>`}
function renderQuestion(s,q){const saved=vals(s,q.id),proposed=saved.length?saved:(q.prefill||[]);return `<div class="chooser familyChoice matrixQuestion103" data-matrix-field="${esc(q.id)}" data-info-kind="${esc(q.kind||'UserChoice')}" data-max="${q.max||3}"><small>${esc(q.q)}</small>${q.help?`<p class="questionHelp">${esc(q.help)}</p>`:''}<input type="hidden" data-family-field="${esc(q.id)}" value="${esc(proposed.join('|||'))}"><div class="chips choiceGrid">${q.opts.map(x=>`<button type="button" class="chip ${proposed.includes(x)?'on':''}" data-family-pick="${esc(q.id)}" data-family-value="${esc(x)}">${esc(x)}</button>`).join('')}</div></div>`}
export function familyDetailsPage105(s){const base=matrix105(s),m=routeQuestions105(s,base);s.metadata=s.metadata||{};s.metadata.matrixFacts105=matrixFacts105(s);s.metadata.router105=m.router105;return `<section class="card familyDetailsStep"><button class="linkBtn" data-action="understanding">→ رجوع</button><div class="muted">الخطوة الثانية</div><h1>أكمل الناقص فقط</h1><p class="lead">الأسئلة هنا ليست قائمة ثابتة؛ تظهر فقط لسد الفجوات المهمة في معنى الوثيقة.</p>${summary(m)}${durationBlock(s,m)}${m.questions.map(q=>renderQuestion(s,q)).join('')}<div class="row"><button class="btn primary" data-action="family-details-next">اعتماد والمتابعة</button></div></section>`}

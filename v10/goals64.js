import {esc} from './engine.js';
import {goalGroups63} from './goals63.js?v=63';
export function goalsPage64(s){const {primary,additional}=goalGroups63(s),selected=s.answers?.goals||[],other=Boolean(s.metadata?.otherGoalEnabled);const button=x=>`<button type="button" class="chip ${selected.includes(x)?'on':''}" data-goal="${esc(x)}">${esc(x)}</button>`;return `<section class="card"><button class="linkBtn" data-action="back-family-details">→ رجوع</button><div class="muted">الخطوة الثالثة</div><h1>الأهداف</h1><p class="lead">هذه أقوى الأهداف المقترحة لهذا العمل. اختر ما ينطبق فعليًا.</p><div class="chips choiceGrid">${primary.map(button).join('')}</div>${additional.length?`<details style="margin-top:14px"><summary class="btn">عرض أهداف إضافية</summary><div class="chips choiceGrid" style="margin-top:12px">${additional.map(button).join('')}</div></details>`:''}<div class="chips choiceGrid" style="margin-top:12px"><button type="button" class="chip ${other?'on':''}" data-action="toggle-other-goal">أخرى</button></div>${other?`<label class="fullField"><span>اكتب الهدف</span><input id="otherGoal" value="${esc(s.metadata?.otherGoal||'')}"></label>`:''}<div class="row"><button class="btn primary" data-action="go-description">التالي: العنوان</button></div></section>`}

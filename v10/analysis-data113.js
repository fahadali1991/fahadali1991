import {esc} from './engine.js';
const arDigits=s=>String(s??'').replace(/[٠-٩]/g,d=>'٠١٢٣٤٥٦٧٨٩'.indexOf(d)).replace(/[۰-۹]/g,d=>'۰۱۲۳۴۵۶۷۸۹'.indexOf(d));
const clean=v=>String(v??'').trim();
export function parseAnalysisRows113(raw,maxScore=0){const rows=String(raw||'').split(/\r?\n/).map(x=>x.trim()).filter(Boolean),scores=[],names=[],invalid=[];for(const row of rows){const normalized=arDigits(row).replace(/\t/g,' ').replace(/[،,;]/g,' ').replace(/\s+/g,' ').trim();const parts=normalized.split(' ');let scoreIndex=-1,score=null;for(let i=parts.length-1;i>=0;i--){const n=Number(parts[i]);if(Number.isFinite(n)){scoreIndex=i;score=n;break}}if(scoreIndex<0||score===null||(Number(maxScore)>0&&(score<0||score>Number(maxScore)))){invalid.push(row);continue}scores.push(score);names.push(parts.slice(0,scoreIndex).join(' ').trim())}return{scores,names,invalid,count:scores.length}}
export function analysisState113(state){state.metadata=state.metadata||{};state.metadata.analysis=state.metadata.analysis||{maxScore:'',scores:[],names:[],rawRows:'',entryMode:'paste'};return state.metadata.analysis}
export function analysisFinding113(state){const a=analysisState113(state),scores=(a.scores||[]).map(Number).filter(Number.isFinite),max=Number(a.maxScore||0);if(!scores.length||!max)return'';const avg=scores.reduce((x,y)=>x+y,0)/scores.length,achievement=avg/max*100,low=scores.filter(x=>x/max<.5).length,advanced=scores.filter(x=>x/max>=.9).length;return `شمل التحليل ${scores.length} طالبًا، وبلغ متوسط الدرجات ${avg.toFixed(2)} من ${max} بنسبة تحصيل ${achievement.toFixed(1)}٪، مع ${low} في مستوى يحتاج تدخلًا مباشرًا و${advanced} في مستوى متقدم.`}
export function analysisSummary113(state){const a=analysisState113(state),scores=(a.scores||[]).map(Number).filter(Number.isFinite),max=Number(a.maxScore||0);if(!scores.length||!max)return{ready:false,count:scores.length,maxScore:max};const sum=scores.reduce((x,y)=>x+y,0),avg=sum/scores.length,sorted=[...scores].sort((x,y)=>x-y),mid=Math.floor(sorted.length/2),median=sorted.length%2?sorted[mid]:(sorted[mid-1]+sorted[mid])/2;return{ready:true,count:scores.length,maxScore:max,sum,avg,median,high:Math.max(...scores),low:Math.min(...scores),achievement:avg/max*100,weak:scores.filter(x=>x/max<.5).length,monitor:scores.filter(x=>x/max>=.5&&x/max<.7).length,advanced:scores.filter(x=>x/max>=.9).length}}
function commit(state,maxScore,rawRows){const a=analysisState113(state);a.maxScore=clean(maxScore);a.rawRows=String(rawRows||'');const parsed=parseAnalysisRows113(rawRows,a.maxScore);a.scores=parsed.scores;a.names=parsed.names;a.invalid=parsed.invalid;a.updatedAt=Date.now();state.metadata.count=String(parsed.count||'');state.metadata.countSource101='derived';state.metadata.familyDetails=state.metadata.familyDetails||{};const finding=analysisFinding113(state);if(finding){state.metadata.familyDetails.finding=finding;state.metadata.analysis.findingSource='derived'}return parsed}
export function analysisDataPending113(state){if(state?.classification?.type!=='تحليل نتائج')return false;return !analysisSummary113(state).ready}
function liveMetrics(a){const sum=analysisSummary113({metadata:{analysis:a}});if(!sum.ready)return `<div class="analysisLive114 empty"><span>ابدأ بكتابة أول درجة</span><small>سيظهر العدد والمتوسط والتحصيل مباشرة.</small></div>`;return `<div class="analysisLive114"><div><small>الطلاب</small><b>${sum.count}</b></div><div><small>المتوسط</small><b>${sum.avg.toFixed(2)}</b></div><div><small>التحصيل</small><b>${sum.achievement.toFixed(1)}٪</b></div><div><small>الأعلى</small><b>${sum.high}</b></div><div><small>الأدنى</small><b>${sum.low}</b></div></div>`}
function manualRows(a){const visible=Math.max(6,a.manualRowCount||a.scores?.length||0);a.manualRowCount=visible;return `<div class="manualHelp114"><b>كتابة يدوية</b><span>الاسم اختياري. اكتب الدرجة واضغط Enter للانتقال مباشرة إلى الطالب التالي.</span></div>${liveMetrics(a)}<div class="analysisTable114"><div class="head"><b>#</b><b>اسم الطالب <small>اختياري</small></b><b>الدرجة</b><b></b></div>${Array.from({length:visible},(_,i)=>`<div class="row114" data-analysis-row114="${i}"><span class="rowNo114">${i+1}</span><input data-analysis-name114="${i}" value="${esc(a.names?.[i]||'')}" placeholder="اسم الطالب"><input data-analysis-score114="${i}" inputmode="decimal" value="${a.scores?.[i]??''}" placeholder="—" aria-label="درجة الطالب ${i+1}"><button type="button" class="removeRow114" data-analysis-remove-row114="${i}" aria-label="حذف الطالب ${i+1}">×</button></div>`).join('')}</div><div class="manualActions114"><button type="button" class="linkBtn addRow114" data-analysis-add-row114>＋ إضافة طالب</button><button type="button" class="linkBtn" data-analysis-add-five114>＋ 5 طلاب</button><span>عدد الطلاب يُحسب تلقائيًا من الدرجات المكتوبة.</span></div>`}
function entryBody(a){const mode=a.entryMode||'paste';if(mode==='table')return manualRows(a);if(mode==='file')return `<label class="analysisFile114"><input type="file" data-analysis-file114 accept=".csv,.tsv,.txt,text/csv,text/tab-separated-values,text/plain"><b>اختر ملف الدرجات</b><span>CSV أو TSV أو TXT</span><small>إذا كان الملف من Excel: «حفظ باسم CSV» ثم ارفعه هنا. لن نرسل الملف لأي خادم.</small></label><div data-analysis-file-feedback114 class="questionHelp">أو ارجع إلى «لصق سريع» والصق الجدول مباشرة من Excel.</div>`;return `<label class="fullField"><span>الدرجات</span><textarea data-analysis-rows113 rows="8" placeholder="35\n28\n40\n...">${esc(a.rawRows||'')}</textarea></label><div class="pasteHint114"><b>أسرع طريقة</b><span>انسخ عمود الدرجات من Excel والصقه كما هو، أو الصق «الاسم + الدرجة».</span></div>`}
export function analysisDataPanel113(state){if(state?.classification?.type!=='تحليل نتائج')return'';const a=analysisState113(state),sum=analysisSummary113(state);if(sum.ready&&!state.metadata?.editAnalysisData113)return `<section class="analysisData113 analysisDataReady113"><div><small>بيانات النتائج</small><b>${sum.count} طالبًا · الدرجة الكلية ${sum.maxScore} · متوسط ${sum.avg.toFixed(2)}</b><p>تم حساب العدد والمؤشرات من الدرجات نفسها؛ لن يطلبها النظام منك مرة أخرى.</p></div><button type="button" class="linkBtn" data-analysis-edit113>تعديل الدرجات</button></section>`;return `<section class="analysisData113" data-analysis-host113><div class="adaptiveKicker106">بيانات التحليل — إدخال مرة واحدة</div><h2>أدخل الدرجات بالطريقة الأسهل لك</h2><p class="questionHelp">يمكنك الكتابة يدويًا بالكامل، أو اللصق من Excel، أو استيراد ملف. كل الطرق تغذي نفس التحليل.</p><label class="fullField"><span>الدرجة الكلية للاختبار</span><input data-analysis-max113 inputmode="decimal" value="${esc(a.maxScore||'')}" placeholder="مثال: 40"></label><div class="analysisModes114"><button type="button" class="mode114 ${(a.entryMode||'paste')==='paste'?'on':''}" data-analysis-mode114="paste"><b>⚡</b><span>لصق سريع</span><small>الأسرع من Excel</small></button><button type="button" class="mode114 ${a.entryMode==='table'?'on':''}" data-analysis-mode114="table"><b>✎</b><span>كتابة يدوية</span><small>طالبًا طالبًا</small></button><button type="button" class="mode114 ${a.entryMode==='file'?'on':''}" data-analysis-mode114="file"><b>⇧</b><span>استيراد ملف</span><small>CSV / TSV / TXT</small></button></div><div data-analysis-entry-body114>${entryBody(a)}</div><div data-analysis-feedback113 class="questionHelp">${sum.count?`تم التعرف على ${sum.count} درجة.`:'لن يظهر حقل منفصل لعدد الطلاب.'}</div><div class="row adaptiveActions106"><button type="button" class="btn primary" data-analysis-next113 ${sum.ready?'':'disabled'}>اعتماد الدرجات والمتابعة</button>${sum.ready?'<button type="button" class="btn" data-analysis-cancel113>إلغاء التعديل</button>':''}</div></section>`}
let current=null;
export function bindAnalysisData113(state){current=state;queueMicrotask(renderAnalysisSlot113)}
function signal(){const carrier=document.querySelector('[data-adaptive-zone] [data-family-field]');if(carrier)carrier.dispatchEvent(new Event('input',{bubbles:true}))}
function tableToRaw(host){const names=[...host.querySelectorAll('[data-analysis-name114]')],scores=[...host.querySelectorAll('[data-analysis-score114]')];return scores.map((el,i)=>{const score=clean(el.value),name=clean(names[i]?.value);return score?`${name?name+' ':''}${score}`:''}).filter(Boolean).join('\n')}
function refreshHost(focusIndex=null){const host=document.querySelector('[data-analysis-host113]');if(!host)return;host.outerHTML=analysisDataPanel113(current);if(focusIndex!==null)queueMicrotask(()=>document.querySelector(`[data-analysis-score114="${focusIndex}"]`)?.focus())}
function updateManualLive(host){const a=analysisState113(current),slot=host.querySelector('.analysisLive114');if(slot){const wrap=document.createElement('div');wrap.innerHTML=liveMetrics(a);slot.replaceWith(wrap.firstElementChild)}}
function validateScoreInput(el,max){const raw=arDigits(el.value),n=Number(raw);el.classList.remove('invalidScore114');el.removeAttribute('aria-invalid');if(!raw)return true;if(!Number.isFinite(n)||n<0||(Number(max)>0&&n>Number(max))){el.classList.add('invalidScore114');el.setAttribute('aria-invalid','true');return false}return true}
export function renderAnalysisSlot113(){if(!current||typeof document==='undefined')return;const slot=document.querySelector('[data-analysis-slot113]'),zone=document.querySelector('[data-adaptive-zone]'),metaPending=Boolean(document.querySelector('[data-family-meta-host111]'));if(!slot)return;if(current.classification?.type!=='تحليل نتائج'){slot.innerHTML='';slot.hidden=true;if(zone)zone.hidden=metaPending;return}slot.hidden=metaPending;if(!metaPending)slot.innerHTML=analysisDataPanel113(current);if(zone)zone.hidden=metaPending||analysisDataPending113(current)||Boolean(current.metadata?.editAnalysisData113)}
if(typeof document!=='undefined'){
 document.addEventListener('family-meta-change111',renderAnalysisSlot113,true);
 document.addEventListener('input',e=>{if(!current)return;const host=e.target.closest?.('[data-analysis-host113]');if(!host)return;const max=host.querySelector('[data-analysis-max113]')?.value||analysisState113(current).maxScore||'';if(e.target.matches('[data-analysis-max113],[data-analysis-rows113]')){const rows=host.querySelector('[data-analysis-rows113]')?.value||analysisState113(current).rawRows||'';const p=commit(current,max,rows),fb=host.querySelector('[data-analysis-feedback113]'),next=host.querySelector('[data-analysis-next113]');if(fb)fb.textContent=p.invalid.length?`تم التعرف على ${p.count} درجة، وهناك ${p.invalid.length} سطر يحتاج مراجعة.`:`تم التعرف على ${p.count} درجة. عدد الطلاب = ${p.count}.`;if(next)next.disabled=!(p.count&&Number(max)>0);return}if(e.target.matches('[data-analysis-name114],[data-analysis-score114]')){if(e.target.matches('[data-analysis-score114]'))validateScoreInput(e.target,max);const raw=tableToRaw(host),p=commit(current,max,raw),fb=host.querySelector('[data-analysis-feedback113]'),next=host.querySelector('[data-analysis-next113]');const invalidCount=host.querySelectorAll('.invalidScore114').length;if(fb)fb.textContent=invalidCount?`راجع ${invalidCount} درجة: يجب أن تكون بين 0 و${max||'الدرجة الكلية'}.`:`تم التعرف على ${p.count} درجة. عدد الطلاب = ${p.count}.`;if(next)next.disabled=!(p.count&&Number(max)>0&&!invalidCount);updateManualLive(host)}},true);
 document.addEventListener('keydown',e=>{if(!current||e.key!=='Enter'||!e.target.matches('[data-analysis-score114]'))return;e.preventDefault();const host=e.target.closest('[data-analysis-host113]'),i=Number(e.target.dataset.analysisScore114),max=host?.querySelector('[data-analysis-max113]')?.value||'';if(!validateScoreInput(e.target,max))return;let next=host.querySelector(`[data-analysis-score114="${i+1}"]`);if(!next){analysisState113(current).manualRowCount=(analysisState113(current).manualRowCount||i+1)+1;refreshHost(i+1);return}next.focus();next.select?.()},true);
 document.addEventListener('change',e=>{if(!current||!e.target.matches('[data-analysis-file114]'))return;const file=e.target.files?.[0],host=e.target.closest('[data-analysis-host113]'),fb=host?.querySelector('[data-analysis-file-feedback114]');if(!file)return;const ok=/\.(csv|tsv|txt)$/i.test(file.name);if(!ok){if(fb)fb.textContent='هذا الإصدار يستورد CSV / TSV / TXT. من Excel اختر «حفظ باسم CSV» أو استخدم اللصق السريع.';return}const r=new FileReader();r.onload=()=>{const text=String(r.result||''),max=host.querySelector('[data-analysis-max113]')?.value||'',p=commit(current,max,text);if(fb)fb.textContent=`تم استيراد ${p.count} درجة من ${file.name}${p.invalid.length?`، و${p.invalid.length} سطر يحتاج مراجعة.`:'.'}`;const status=host.querySelector('[data-analysis-feedback113]'),next=host.querySelector('[data-analysis-next113]');if(status)status.textContent=`عدد الطلاب = ${p.count}.`;if(next)next.disabled=!(p.count&&Number(max)>0)};r.readAsText(file,'utf-8')},true);
 document.addEventListener('click',e=>{if(!current)return;const mode=e.target.closest('[data-analysis-mode114]');if(mode){analysisState113(current).entryMode=mode.dataset.analysisMode114;if(mode.dataset.analysisMode114==='table'&&!analysisState113(current).manualRowCount)analysisState113(current).manualRowCount=6;refreshHost(mode.dataset.analysisMode114==='table'?0:null);return}const add=e.target.closest('[data-analysis-add-row114]');if(add){const a=analysisState113(current);a.manualRowCount=(a.manualRowCount||Math.max(6,a.scores?.length||0))+1;refreshHost(a.manualRowCount-1);return}const addFive=e.target.closest('[data-analysis-add-five114]');if(addFive){const a=analysisState113(current);a.manualRowCount=(a.manualRowCount||Math.max(6,a.scores?.length||0))+5;refreshHost(a.manualRowCount-5);return}const remove=e.target.closest('[data-analysis-remove-row114]');if(remove){const host=remove.closest('[data-analysis-host113]'),idx=Number(remove.dataset.analysisRemoveRow114),names=[...host.querySelectorAll('[data-analysis-name114]')],scores=[...host.querySelectorAll('[data-analysis-score114]')];names[idx].value='';scores[idx].value='';const raw=tableToRaw(host),max=host.querySelector('[data-analysis-max113]')?.value||'',p=commit(current,max,raw);analysisState113(current).manualRowCount=Math.max(1,(analysisState113(current).manualRowCount||6)-1);refreshHost(Math.max(0,Math.min(idx,analysisState113(current).manualRowCount-1)));return}if(e.target.closest('[data-analysis-edit113]')){current.metadata.editAnalysisData113=true;renderAnalysisSlot113();return}if(e.target.closest('[data-analysis-cancel113]')){current.metadata.editAnalysisData113=false;renderAnalysisSlot113();return}const next=e.target.closest('[data-analysis-next113]');if(next&&!next.disabled){current.metadata.editAnalysisData113=false;signal();renderAnalysisSlot113()}},true)
}
if(typeof document!=='undefined'&&!document.getElementById('analysis-entry114-style')){const s=document.createElement('style');s.id='analysis-entry114-style';s.textContent='.analysisModes114{display:grid;grid-template-columns:repeat(3,1fr);gap:9px;margin:12px 0}.mode114{border:1px solid #d9e6e1;background:#fff;border-radius:15px;padding:12px 8px;display:flex;flex-direction:column;align-items:center;gap:3px;color:#173f38}.mode114 b{font-size:22px;color:#0da9a6}.mode114 span{font-weight:800}.mode114 small{font-size:10px;color:#788984}.mode114.on{border:2px solid #07a869;background:#f1faf6;box-shadow:0 5px 18px rgba(7,168,105,.12)}.pasteHint114,.manualHelp114{margin-top:8px;padding:10px 12px;border-radius:12px;background:#f5faf8;border:1px solid #dfece7;display:flex;gap:8px;flex-wrap:wrap;align-items:center}.pasteHint114 b,.manualHelp114 b{color:#087c5c}.manualHelp114 span{color:#667b74;font-size:12px}.analysisLive114{display:grid;grid-template-columns:repeat(5,1fr);gap:7px;margin:10px 0}.analysisLive114>div{border:1px solid #dce9e4;background:#fff;border-radius:11px;padding:8px;text-align:center}.analysisLive114 small{display:block;color:#768781;font-size:9px}.analysisLive114 b{display:block;color:#087c5c;font-size:16px;margin-top:2px}.analysisLive114.empty{display:flex;flex-direction:column;align-items:flex-start;background:#fbfdfc;border:1px dashed #c9ddd6;border-radius:11px;padding:10px}.analysisLive114.empty small{margin-top:3px}.analysisTable114{border:1px solid #dfe9e5;border-radius:12px;overflow:hidden}.analysisTable114 .head,.analysisTable114 .row114{display:grid;grid-template-columns:42px 1.8fr .7fr 42px;align-items:center}.analysisTable114 .head{background:#eef7f4;padding:8px 0}.analysisTable114 .head b{text-align:center;font-size:12px}.analysisTable114 .head small{font-weight:400;color:#7b8984}.analysisTable114 .row114{border-top:1px solid #e8efec}.analysisTable114 input{border:0!important;border-radius:0!important;background:#fff!important;min-height:44px}.analysisTable114 input+input{border-right:1px solid #e8efec!important;text-align:center}.rowNo114{text-align:center;font-size:11px;color:#78908a}.removeRow114{border:0;background:transparent;color:#a06b6b;font-size:20px;cursor:pointer}.invalidScore114{background:#fff3f3!important;box-shadow:inset 0 0 0 1px #d56b6b!important}.manualActions114{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-top:8px}.manualActions114 span{font-size:11px;color:#74847f;margin-right:auto}.analysisFile114{border:2px dashed #b7d8cd;border-radius:16px;min-height:150px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:6px;background:#f8fcfa;cursor:pointer;text-align:center;padding:18px}.analysisFile114 input{display:none}.analysisFile114 b{font-size:16px;color:#087c5c}.analysisFile114 span{font-weight:800}.analysisFile114 small{max-width:420px;color:#73827d}@media(max-width:600px){.analysisModes114{grid-template-columns:1fr}.mode114{flex-direction:row;justify-content:flex-start;text-align:right}.mode114 b{width:32px}.mode114 small{margin-right:auto}.analysisLive114{grid-template-columns:repeat(3,1fr)}.analysisTable114 .head,.analysisTable114 .row114{grid-template-columns:34px 1.4fr .8fr 34px}.manualActions114 span{width:100%;margin:0}}';document.head.appendChild(s)}
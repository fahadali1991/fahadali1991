import {esc,joinAr,familyOptions,subtypeOptions} from './engine.js';
const AUD=['الطلاب','المعلمون','أولياء الأمور','الإداريون','جميع منسوبي المدرسة','مستفيدون آخرون'];
const DUR=['','حصة دراسية','حصتان دراسيتان','يوم دراسي','يومان','أسبوع','أسبوعان','شهر','فصل دراسي','مدة أخرى'];
const PLC=['','فصل دراسي','مركز مصادر التعلم','المكتبة','معمل الحاسب','المختبر','المسرح المدرسي','المصلى','ساحة المدرسة','الملعب','غرفة النشاط','قاعة التدريب','مكتب الإدارة','عن بُعد','خارج المدرسة','مكان آخر'];
const DOM=['','البرامج والأنشطة','الإدارة والمتابعة','التحصيل والتقويم','التخطيط والتحسين','الحضور والانضباط','التطوير المهني','التوجيه الطلابي','الأمن والسلامة','الشراكة المجتمعية','الجودة والتقويم الذاتي','التقنية والتحول الرقمي','القيم والهوية الوطنية'];
const opts=(a,v,p)=>a.map(x=>`<option value="${esc(x)}" ${x===v?'selected':''}>${esc(x||p)}</option>`).join('');
export function understanding55(s){const c=s.classification,suggest=s.suggestedAudiences?.length?`<p class="questionHelp">قد يكون المستفيدون: <b>${esc(joinAr(s.suggestedAudiences))}</b>. اختر بنفسك للتأكيد.</p>`:'<p class="questionHelp">اختر المستفيدين الفعليين.</p>';return `<section class="card"><div class="muted">الخطوة الأولى</div><h1>راجع ما فهمه المحرك</h1><p class="lead">صحح التصنيف والبيانات فقط. سنبني العنوان بعد اختيار الأهداف.</p><section class="classificationBlock"><div class="muted">تصنيف العمل</div><div class="chooser"><small>عائلة العمل</small><div class="chips">${familyOptions().map(x=>`<button type="button" class="chip ${x===c.type?'on':''}" data-type="${esc(x)}">${esc(x)}</button>`).join('')}</div></div><div class="chooser"><small>النوع الفرعي</small><div class="chips">${subtypeOptions(s).map(x=>`<button type="button" class="chip ${x===c.subtype?'on':''}" data-subtype="${esc(x)}">${esc(x)}</button>`).join('')}</div></div><label class="fullField"><span>المجال</span><select id="domainSelect">${opts(DOM,c.domain||'','اختر المجال')}</select></label></section><div class="editGrid"><label><span>المنفذ</span><input id="executorName" value="${esc(s.metadata.executorName||'')}"></label><label><span>العدد</span><input id="count" inputmode="numeric" value="${esc(s.metadata.count||'')}"></label><label><span>التاريخ</span><input id="dateISO" type="date" value="${esc(s.metadata.dateISO||'')}"></label></div><div class="editGrid"><label><span>المدة</span><select id="durationSelect">${opts(DUR,s.metadata.durationChoice||'','اختر المدة')}</select></label><label><span>مكان التنفيذ</span><select id="placeSelect">${opts(PLC,s.metadata.placeChoice||'','اختر المكان')}</select></label></div>${s.metadata.durationChoice==='مدة أخرى'?`<label class="fullField"><span>اكتب المدة</span><input id="customDuration" value="${esc(s.metadata.customDuration||'')}"></label>`:''}${s.metadata.placeChoice==='مكان آخر'?`<label class="fullField"><span>اكتب المكان</span><input id="customPlace" value="${esc(s.metadata.customPlace||'')}"></label>`:''}<div class="chooser"><small>المستفيدون</small>${suggest}<div class="chips choiceGrid">${AUD.map(x=>`<button type="button" class="chip ${(s.audiences||[]).includes(x)?'on':''}" data-audience="${esc(x)}">${esc(x)}</button>`).join('')}</div>${(s.audiences||[]).includes('مستفيدون آخرون')?`<label class="fullField"><span>من هم؟</span><input id="otherAudience" value="${esc(s.metadata.otherAudience||'')}"></label>`:''}</div><div class="row"><button class="btn primary" data-action="go-goals">التالي: الأهداف</button></div></section>`}

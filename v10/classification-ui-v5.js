import {esc,familyOptions,subtypeOptions} from './engine.js';
const DOMAINS=['','البرامج والأنشطة','الإدارة والمتابعة','التحصيل والتقويم','التخطيط والتحسين','الحضور والانضباط','التطوير المهني','التوجيه الطلابي','الأمن والسلامة','الشراكة المجتمعية','الجودة والتقويم الذاتي','التقنية والتحول الرقمي','القيم والهوية الوطنية'];
const opts=(a,v,p)=>a.map(x=>`<option value="${esc(x)}" ${x===v?'selected':''}>${esc(x||p)}</option>`).join('');
export function classificationSection(s){const c=s.classification;return `<div class="chooser"><small>تصنيف العمل</small><p class="questionHelp">راجع التصنيف وعدله إذا لزم.</p><div class="classificationBlock"><div><b class="fieldTitle">عائلة العمل</b><div class="chips">${familyOptions().map(x=>`<button class="chip ${x===c.type?'on':''}" data-type="${esc(x)}">${esc(x)}</button>`).join('')}</div></div><div><b class="fieldTitle">النوع الفرعي</b><div class="chips">${subtypeOptions(s).map(x=>`<button class="chip ${x===c.subtype?'on':''}" data-subtype="${esc(x)}">${esc(x)}</button>`).join('')}</div></div><div class="editGrid"><label><span>المجال</span><select id="domainSelect">${opts(DOMAINS,c.domain||'','اختر المجال')}</select></label><label><span>الموضوع</span><input id="topicEdit" value="${esc(s.topic||'')}" placeholder="اكتب موضوع العمل أو عدله"></label></div></div></div>`}

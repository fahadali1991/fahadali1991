import {esc} from './engine.js';
import {descriptionVariants76} from './description-variants76.js?v=76';
export function descriptionChoicesPage76(state){const variants=descriptionVariants76(state);return `<section class="card"><button class="linkBtn" data-action="go-goals">→ رجوع</button><div class="muted">صياغة التوثيق</div><h1>اختر صيغة وصف التنفيذ</h1><p class="lead">أعددت لك أربع صيغ، ويستخدم النص المجال والمؤشر اللذين اعتمدتهما.</p><div class="variantGrid">${variants.map(v=>`<article class="variantCard"><div class="variantHead"><b>${esc(v.label)}</b><small>${esc(v.help)}</small></div>${v.mode==='bullets'?`<ul class="smartList">${v.items.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`:`<p>${esc(v.text)}</p>`}<button class="btn primary" data-description-variant="${esc(v.id)}">اختيار هذا النموذج</button></article>`).join('')}</div></section>`}
export function descriptionEditorPage76(state){const text=state.metadata.generatedDescription||'',label=state.metadata.descriptionVariantLabel||'النموذج المختار';return `<section class="card"><button class="linkBtn" data-action="description-choices">→ رجوع للنماذج</button><div class="muted">النموذج المختار: ${esc(label)}</div><h1>وصف التنفيذ</h1><p class="lead">راجع النص وعدله، ثم سيمر على مراجعة الإملاء واللغة وعلامات الترقيم قبل الشواهد.</p><textarea id="generatedDescription" class="descriptionEditor" spellcheck="true" autocorrect="on">${esc(text)}</textarea><div class="row"><button class="btn primary" data-action="go-evidence">مراجعة النص والمتابعة</button><button class="btn" data-action="description-choices">اختيار نموذج آخر</button></div></section>`}

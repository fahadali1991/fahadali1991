import {esc} from './engine.js';
import {descriptionVariants73} from './description-variants73.js?v=73';
export function descriptionChoicesPage73(state){const variants=descriptionVariants73(state);return `<section class="card"><button class="linkBtn" data-action="go-goals">→ رجوع</button><div class="muted">صياغة التوثيق</div><h1>اختر صيغة وصف التنفيذ</h1><p class="lead">أعددت لك أربع صيغ مختلفة. إذا وجد المحرك ارتباطًا قويًا من القاموس الكامل فسيظهر بصورة مهنية في الصيغ المناسبة.</p><div class="variantGrid">${variants.map(v=>`<article class="variantCard"><div class="variantHead"><b>${esc(v.label)}</b><small>${esc(v.help)}</small></div>${v.mode==='bullets'?`<ul class="smartList">${v.items.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`:`<p>${esc(v.text)}</p>`}<button class="btn primary" data-description-variant="${esc(v.id)}">اختيار هذا النموذج</button></article>`).join('')}</div></section>`}
export function descriptionEditorPage73(state){const text=state.metadata.generatedDescription||'',label=state.metadata.descriptionVariantLabel||'النموذج المختار';return `<section class="card"><button class="linkBtn" data-action="description-choices">→ رجوع للنماذج</button><div class="muted">النموذج المختار: ${esc(label)}</div><h1>وصف التنفيذ</h1><p class="lead">راجع النص وعدله قبل الانتقال إلى الشواهد. سيعمل المدقق اللغوي على النص بعد ذلك.</p><textarea id="generatedDescription" class="descriptionEditor" spellcheck="true" autocorrect="on">${esc(text)}</textarea><div class="row"><button class="btn primary" data-action="go-evidence">مراجعة النص والمتابعة</button><button class="btn" data-action="description-choices">اختيار نموذج آخر</button></div></section>`}

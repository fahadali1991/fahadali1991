import {esc} from './engine.js';
import {familyRequirements} from './family-requirements57.js';
const value=(s,id)=>String(s.metadata?.familyDetails?.[id]||'');
function field(s,f){const v=value(s,f.id);if(f.type==='textarea')return `<label class="fullField familyField"><span>${esc(f.label)}</span><textarea data-family-field="${esc(f.id)}" placeholder="${esc(f.placeholder||'')}">${esc(v)}</textarea></label>`;if(f.type==='select')return `<label class="fullField familyField"><span>${esc(f.label)}</span><select data-family-field="${esc(f.id)}"><option value="">اختر</option>${(f.options||[]).map(x=>`<option value="${esc(x)}" ${x===v?'selected':''}>${esc(x)}</option>`).join('')}</select></label>`;return `<label class="fullField familyField"><span>${esc(f.label)}</span><input data-family-field="${esc(f.id)}" value="${esc(v)}" placeholder="${esc(f.placeholder||'')}"></label>`}
export function familyDetailsPage(s){const r=familyRequirements(s.classification.type);if(!r.fields.length)return `<section class="card"><button class="linkBtn" data-action="understanding">→ رجوع</button><div class="muted">الخطوة الثانية</div><h1>${esc(r.label)}</h1><p class="lead">لا توجد بيانات إضافية إلزامية لهذه العائلة. سننتقل للأهداف.</p><div class="row"><button class="btn primary" data-action="family-details-next">التالي: الأهداف</button></div></section>`;return `<section class="card familyDetailsStep"><button class="linkBtn" data-action="understanding">→ رجوع</button><div class="muted">الخطوة الثانية</div><h1>${esc(r.label)}</h1><p class="lead">هذه البيانات تختلف حسب نوع الوثيقة. اكتب الموجود فعليًا فقط، واترك غير المتوفر فارغًا.</p><div class="familyFields">${r.fields.map(f=>field(s,f)).join('')}</div><div class="helperBox"><b>الشواهد الأنسب لهذه العائلة</b><p>${r.evidence.map(esc).join(' • ')}</p></div><div class="row"><button class="btn primary" data-action="family-details-next">التالي: الأهداف</button></div></section>`}

# سياسة نشر الروابط — V2

## الهدف
منع إرسال روابط تطوير قديمة أو غير متزامنة مع ملفات JavaScript/CSS.

## القاعدة الإلزامية
1. لا يُرسل للمستخدم أي رابط يعتمد على فرع متغير مثل `/main/` أو `/master/`.
2. كل رابط اختبار/عرض للمستخدم يجب أن يكون مبنيًا على **Commit SHA ثابت**.
3. رابط العرض المفضل:
   `https://rawcdn.githack.com/<owner>/<repo>/<commit-sha>/<file.html>`
4. قبل إرسال الرابط يجب التحقق من GitHub أن:
   - ملف HTML موجود في نفس الـcommit.
   - كل ملفات CSS/JS المشار إليها من HTML موجودة في نفس الـcommit.
   - آخر إصلاح مقصود موجود في HTML/الملف المعني لذلك الـcommit.
5. لا تعتمد Query String لكسر الكاش في رابط الإنتاج؛ الـcommit نفسه هو إصدار الكاش.
6. `/main/` يستخدم داخليًا للتطوير فقط، ولا يرسل للمستخدم.
7. عند أي تعديل جديد يتم إنشاء Release Build جديد برقم/commit جديد، ولا يتم تغيير الرابط القديم.
8. إذا تعذر التحقق من وصول CDN خارجي من بيئة التنفيذ، يجب التصريح بذلك وعدم الادعاء بفحص HTTP خارجي، مع الاعتماد على تحقق GitHub وبنية الرابط الثابت.

## سجل الإصدار الحالي
- app: `school-engine-v9-guided-capture-lab.html`
- stable build commit: `b49d7ec08c8af6fca109e2c583d1e84814744001`
- يتضمن: Create Document Guard، Icon Guard، G0 Diagnostics، Stability V2، Print V2.

## مبدأ
الرابط ليس "آخر main"؛ الرابط هو **نسخة إصدار محددة لا تتغير**.
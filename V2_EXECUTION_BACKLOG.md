# V2 EXECUTION BACKLOG — محرك التوثيق المدرسي الذكي

> خطة التنفيذ المرحلية المستخرجة من `MASTER_FEATURE_REGISTER_V2.md` وتخضع لدستور المنتج وخريطة النظام.

## قاعدة التنفيذ
لا تبدأ ميزة في مرحلة لاحقة قبل اجتياز بوابة المرحلة السابقة. لا تتحول حالة أي ميزة إلى «منفذة» لمجرد وجود الكود؛ يجب اجتياز معيار القبول والاختبار على iPhone والحاسب عند ارتباط الميزة بالواجهة أو المخرج.

---

# المرحلة 0 — تثبيت المختبر P0
**الهدف:** جعل النسخة الحالية منصة اختبار موثوقة قبل نقل المنطق إلى V2 النظيفة.

## 0.1 الطباعة والمخرج
1. `ST-02` طباعة A4 على iPhone — تثبيت المسار الحالي.
2. `ST-03` حفظ PDF مطابق للمعاينة.
3. `ST-04` مقياس طباعة تلقائي — معالجة التكبير الذي ظهر في الاختبار الأخير.
4. `ST-05` منع انتقال التوقيعات وحدها إلى صفحة ثانية.
5. اختبار 1/2/3/4 صور + نص قصير/متوسط/طويل.

**معيار الإغلاق:** خمس حالات طباعة متتالية ناجحة على iPhone + فحص حاسب، دون قص أو صفحة شبه فارغة.

## 0.2 الحالة والشواهد
1. `ST-06` حفظ الصور أثناء التنقل والتعديل.
2. `ST-07` عدم فقد البيانات عند تعديل نوع الوثيقة.
3. `EV-01` إعادة اختبار الإرفاق قبل وبعد التوليد.

**معيار الإغلاق:** سيناريو تعديل كامل لا يفقد حقلاً صحيحًا أو صورة.

## 0.3 QA
1. `ST-01` اختبار جميع الأزرار والمسارات.
2. `ST-08` Syntax + Smoke + Regression.
3. إنشاء مجموعة حالات قبول ثابتة تحفظ للمقارنة بين الإصدارات.

**بوابة G0:** لا عطل Blocking معروف في الإدخال، التعديل، الإرفاق، التوليد، الطباعة، PDF.

---

# المرحلة 1 — نواة V2 P1
**الهدف:** التوقف عن بناء الذكاء فوق سلسلة patches، ونقل القواعد المثبتة إلى وحدات مستقلة.

## 1.1 الهيكل
1. `ST-09` إنشاء بنية V2 مستقلة تدريجيًا.
2. وحدات البداية:
   - `UnderstandingEngine`
   - `DocumentRegistry`
   - `GuidedCapture`
   - `ContentEngine`
   - `OutputEngine`
   - `StateStore`
3. إبقاء V1 مرجعًا للسلوك الناجح أثناء النقل، لا أساسًا دائمًا.

## 1.2 Document Registry
تنفيذ `DR-01` وما يتبعه لكل وثيقة إثبات:
- id والاسم المألوف.
- العائلة.
- متى تستخدم.
- إشارات اللغة الطبيعية.
- البيانات المستنتجة.
- الأسئلة الضرورية.
- قواعد التحقق.
- renderer.
- الشواهد المناسبة.
- التوقيعات.
- السابق/التالي.

## 1.3 أربع عائلات إثبات فقط
لا توسع قبل نجاحها:
1. **برنامج/فعالية** — `OUT-02`.
2. **محضر اجتماع** — `OUT-05`.
3. **خطة** — `OUT-04`، تبدأ بالعلاجية ثم تثبت قابلية التوسع.
4. **تحليل نتائج** — `OUT-03` + أساس `AN-01`.

**سبب الاختيار:** ليست محور المشروع؛ هي أربع بنيات مختلفة تكشف هل المعمارية عامة فعلًا.

**بوابة G1:** نفس Design System، لكن أربع هندسات إخراج مختلفة تعمل من Registry دون منطق مكرر خاص بالصفحة الرئيسية.

---

# المرحلة 2 — الفهم والإكمال الذكي P1
**الهدف:** أن يفهم النظام قبل أن يسأل.

## 2.1 Understanding
ترتيب التنفيذ:
1. `UN-01` اللغة الطبيعية والعامية.
2. `UN-05` عائلة العمل.
3. `UN-06` النوع الفرعي.
4. `UN-07` فهم «خطة» بالسياق.
5. `UN-08/09` المستفيدون والصفوف المركبة.
6. `UN-14` درجة الثقة وقابلية التصحيح.
7. `UN-04` الغموض → 2–4 خيارات قابلة للضغط.
8. `UN-12/13` Topic Intelligence والغرض التربوي.

## 2.2 Guided Capture
1. `GC-01` المدخلان إلى نفس المحرك.
2. `GC-02` سؤال ما ينقص فقط.
3. تثبيت `GC-03/04/05` متعدد الاختيار و«أخرى» والحصرية.
4. `GC-06` «هذا ما سأعده لك».
5. `GC-07` تعديل الاستنتاجات.
6. `GC-08` عدم إعادة طلب المعروف.

## 2.3 المحتوى
1. `CT-01/04` عنوان مناسب للعائلة.
2. `CT-05` أهداف متخصصة بالموضوع.
3. `CT-06` خطوات تنفيذ متخصصة بلا اختلاق.
4. `CT-07` وصف تنفيذي مختصر.
5. `CT-08/09` المخرجات والأثر وفق الحقائق فقط.
6. `CT-10` إزالة التكرار.

**معيار القبول:** عينة مدرسية متنوعة لا تقل عن 50 حالة، مع هدف لاحق للوصول إلى 90%+ في تصنيف النية، ومراجعة بشرية لجودة النص وعدم اختلاق الحقائق.

---

# المرحلة 3 — جودة الوثيقة والشواهد P1/P2
**الهدف:** الانتقال من «مولد» إلى «محرك توثيق يتحقق».

## 3.1 Evidence Engine
1. `EV-02` حدوث/منتج/نتيجة/أثر.
2. اقتراح نوع الشاهد المناسب حسب Registry.
3. وصف شاهد قابل للتعديل.
4. `EV-09` كشف نوع الدليل الناقص.
5. لاحقًا `EV-05` فحص صلة الصورة بصريًا، دون منع المستخدم.

## 3.2 Quality Gate
1. `QG-01` العنوان ↔ الأهداف.
2. `QG-02` الأهداف ↔ التنفيذ.
3. المستفيدون ↔ المحتوى.
4. المخرجات ↔ الحقائق.
5. `QG-05` منع أثر بلا دليل.
6. جاهزية الوثيقة + ملاحظات قصيرة قابلة للإصلاح.

**بوابة G2:** النظام يستطيع اكتشاف وثيقة متناقضة أو أثر غير مدعوم أو ملف شواهد يثبت التنفيذ فقط.

---

# المرحلة 4 — الترابط وسير العمل P2
**الهدف:** ألا تنتهي قيمة النظام عند PDF.

1. `WF-01` توريث البيانات.
2. `WF-02` عرض خطوة تالية واحدة فقط.
3. `WF-03` تحليل → علاج/إثراء → إعادة قياس.
4. تقويم ذاتي → تحسين → متابعة.
5. احتياج مهني → نشاط → تطبيق → أثر.
6. حالة طالب → إجراء/تواصل → متابعة.

**بوابة G3:** إنشاء الوثيقة التالية من السابقة دون إعادة إدخال الحقائق المشتركة.

---

# المرحلة 5 — المدرسة كوحدة P2/P3
**الهدف:** تحويل الوثائق إلى ذاكرة تنظيمية وجاهزية.

1. `IN-01` اقتراح مؤشر مع السبب، دون حكم تحقق.
2. `IN-03` ملف المؤشر الذكي.
3. `AR-01` الأرشيف الموحد.
4. البحث بالعام/الفصل/النوع/الخطة/المؤشر/المسؤول/الحالة.
5. `AR-05` لوحة الجاهزية.
6. لاحقًا `AR-04` تقرير الإنجاز التلقائي من الأرشيف.

**بوابة G4:** المدير يستطيع معرفة المكتمل والناقص والمتأخر وضعف الأدلة دون فتح الملفات واحدًا واحدًا.

---

# مؤجل عمدًا حتى ما بعد G4
- الإدخال الصوتي.
- التقويم المدرسي الذكي الشامل.
- QR المتقدم.
- التكامل المتقدم مع نور/Excel بعد إثبات تحليل النتائج الأساسي.
- التوسع السريع في كل عائلات الأمن والسلامة والصيانة والحقوق قبل استقرار Registry.
- أي ميزة شكلية لا تحسن الفهم أو التوثيق أو الجودة أو الترابط أو الجاهزية.

---

# ترتيب العمل الفعلي من هذه اللحظة
1. إغلاق `ST-04` مقياس الطباعة على iPhone.
2. إغلاق `ST-03/ST-05` PDF والصفحات شبه الفارغة.
3. اختبار `ST-06/ST-07` الصور والحالة.
4. تثبيت QA لـ G0.
5. بعد نجاح G0 فقط: بدء Refactor نواة V2 وRegistry.
6. إثبات العائلات الأربع.
7. ثم Topic Intelligence + Quality/Evidence.

## قاعدة منع الانجراف
تقرير البرنامج وتحليل النتائج والمحضر والخطة **عينات اختبار معمارية فقط**. لا يجوز أن تتحول خارطة الطريق إلى تطوير هذه الوثائق على حساب المحرك العام.
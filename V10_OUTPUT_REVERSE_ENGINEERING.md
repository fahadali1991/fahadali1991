# V10 Output Document — Reverse Engineering Specification

## الهدف
هذه الوثيقة تحدد المخرج النهائي أولًا ثم تعكسه إلى البيانات والأسئلة التي يجب أن يجمعها المحرك. الغرض هو منع تضخم النماذج وضمان أن كل سؤال يظهر للمستخدم له أثر مباشر في الوثيقة النهائية.

## شكل الوثيقة النهائي
1. عنوان ذكي للوثيقة.
2. بيانات التنفيذ حسب عائلة الوثيقة.
3. الأهداف في نقاط واضحة.
4. قسم نصي واحد باسم «وصف التنفيذ ومخرجاته» يدمج الوصف، إجراءات التنفيذ، والمخرجات الفعلية دون تكرار.
5. الشواهد في نقاط واضحة.
6. لا يوجد قسم مستقل باسم «المتابعة» في المخرج النهائي.

## قاعدة العنوان الذكي
العنوان لا يعتمد على اسم العائلة فقط. يبنى من: النوع الفرعي + الموضوع + الهدف/الإجراء الأبرز + الفئة/الصف عند الحاجة. يعرض النظام اقتراحات ويظل للمستخدم حق التعديل.

## المستفيدون
المحرك يمكنه الاستدلال للمساعدة فقط، لكنه لا يعتمد المستفيدين تلقائيًا. يجب أن يؤكد المستخدم المستفيدين باختيار صريح، ويمكن اختيار أكثر من فئة.

## البيانات المشتركة
- عنوان الوثيقة
- المنفذ/المعد
- التاريخ: هجري أولًا ثم الميلادي
- مكان التنفيذ عند انطباقه
- المدة عند انطباقها
- المستفيدون
- العدد عند انطباقه
- الصفوف/المرحلة عند انطباقها

## بيانات تتغير حسب العائلة
- برنامج/فعالية: المكان، المدة، المستفيدون، العدد، الصفوف.
- اجتماع: رئيس الاجتماع، معد المحضر، المكان، وقت البداية والنهاية، الحضور.
- تحليل نتائج: المادة، الصف، نوع الاختبار، الفترة، عدد الطلاب/المختبرين.
- خطة: المعد/المسؤول، البداية، النهاية، الفريق، الفئة.
- تطوير مهني: الجهة، الساعات، نمط التنفيذ، الشهادة أو مقدم النشاط بحسب النوع.
- متابعة: المسؤول، الفترة، الحالات/العدد، وسيلة المتابعة.

## الهندسة العكسية للأسئلة
كل عنصر في الورقة يجب أن يكون له مصدر بيانات معروف:
- العنوان ← فهم النص + التصنيف + الموضوع + الأهداف + تعديل المستخدم.
- الأهداف ← مجموعة goal أو purpose بحسب العائلة، وتعرض كنقاط لا كفقرة.
- وصف التنفيذ ومخرجاته ← النص الأصلي + reason/basis + method/work/action + participation/finding + product/outcome.
- الشواهد ← نوع العائلة + المخرجات + ما يضيفه المستخدم من مرفقات لاحقًا.
- بيانات التنفيذ ← metadata والاختيارات المؤكدة.

## قاعدة الجودة
لا تكتب حقيقة لم يذكرها المستخدم أو يؤكدها. لا تحول اختيارًا مستقبليًا إلى إنجاز وقع بالفعل. لا تكرر نفس الاختيار في الأهداف والوصف والمخرجات بصيغ مختلفة.

## اختبار المستخدم قليل الخبرة
يجب أن يستطيع المعلم فهم كل سؤال دون مصطلح تقني، والاختيار بلمسة واحدة، والعودة دون فقد البيانات. إذا احتاج شرحًا طويلًا فالتصميم يحتاج تبسيطًا.
